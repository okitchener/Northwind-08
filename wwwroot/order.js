// // if orders index.html required date is later than today add the class of overdue to the tr element
// document.addEventListener("DOMContentLoaded", function() {
//     const today = new Date();
//     document.querySelectorAll(".required-date").forEach(function(element) {
//         const requiredDate = new Date(element.innerHTML);
//         if (requiredDate < today) {
//             element.classList.add("overdue");
//         }
//     });
// });
